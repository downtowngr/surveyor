<h1>Polls</h1>

<%= link_to 'Create Poll', new_poll_path %>

<table>
  <%= @polls.each do |poll| %>
    <tr>
      <td><%= link_to poll.name, poll %></td>
      <td><%= poll.description %></td>
    </tr>
  <% end %>
</table>


