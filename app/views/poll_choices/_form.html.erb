<%= form_for [:poll, @poll_choice] do |f| %>

  <div class="form-entry">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="form-entry">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <%= f.submit %>
<% end %>
